---
interface Props {
    sheet: URL|string;
    x?: number;
    y?: number;
    width: number;
    height: number;
    title?: string;
}

const { sheet, x = 0, y = 0, width, height, title = "" } = Astro.props;
const backgroundImageUrl = `${sheet}`;
---
<figure class="sprite-image" title=`${title}`/>

<style define:vars={{
    'background-image-url': `url(${backgroundImageUrl})`,
    'x': `${-1 * x}px`,
    'y': `${-1 * y}px`,
    'width': `${width}px`,
    'height': `${height}px`,
}}>
    .sprite-image {
        display: inline-block;
        width: var(--width);
        height: var(--height);

        background-image: var(--background-image-url);
        background-position-x: var(--x);
        background-position-y: var(--y);
        background-repeat: no-repeat;
    }
</style>